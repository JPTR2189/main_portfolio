/** Cubic-bezier curve used across the site for smooth, premium-feeling motion. */
export const PREMIUM_EASE: [number, number, number, number] = [0.22, 1, 0.36, 1];

/** Delay (in seconds) between each staggered item animation. */
export const STAGGER_DELAY = 0.05;
